# ingest.py runs on the Python standard library only (no installs needed).
# The embedding / vector-store step (embed.py) needs these:
psycopg[binary]>=3.1      # Postgres client
pgvector>=0.2             # pgvector adapter
openai>=1.0               # or your embeddings provider of choice
tiktoken>=0.5             # token counting for chunk sizing
# Optional, for richer parsing / bulk case-law work:
lxml>=5.0
